# Local environment variables (copy to .env.local and customize)
# This file is committed to git as an example
# .env.local is ignored by git and should contain your actual values

# Override API URL for local development if needed
# VITE_API_BASE_URL=http://192.168.1.100:8080/api

# Override app title for local development
# VITE_APP_TITLE=Awesome Assistant (Local)

# Add any other local-specific variables here